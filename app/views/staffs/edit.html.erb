<%= render 'menu' %>
<% provide(:title, 'Edit Staff Member') %>
<h1 align="center">Edit Staff Member</h1>

<%= render "layouts/header" %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with model: @staff, local: true do |f| %>

      <%= render 'error_messages' %>

      <%= f.label "First Name"  %>
      <%= f.text_field :fName, class: 'form-control' %>

      <%= f.label "Last Name" %>
      <%= f.text_field :lName, class: 'form-control' %>

      <%= f.label "Grade" %>
      <%= f.text_field :gradeTaught, class: 'form-control' %>

      <%= f.label "Email" %>
      <%= f.email_field :email, class: 'form-control' %>

      <%= f.label "Password" %>
      <%= f.password_field :password, class: 'form-control' %>

      <%= f.label "Confirmation" %>
      <%= f.password_field :password_confirmation, class: 'form-control' %>
      <br/>

      <%= f.submit "Edit Staff Member", class: "btn btn-primary" %>
      <%= link_to 'Return to Main', admin_path, class: "btn btn-primary"  %>
    <% end %>
  </div>
</div>
